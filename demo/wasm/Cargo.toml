[package]
name = "box3d-wasm"
version = "0.1.0"
edition = "2021"
description = "WASM bindings for the Box3D Rust physics engine demos"
license = "MIT"

[lib]
crate-type = ["cdylib"]

[dependencies]
box3d-rust = { path = "../.." }
wasm-bindgen = "0.2"
