{
  "name": "box3d-demo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "bun --watch run dev.ts",
    "build:wasm": "powershell -File build-wasm.ps1",
    "build": "bun run build.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/three": "^0.185.1"
  },
  "dependencies": {
    "three": "^0.185.1"
  }
}
